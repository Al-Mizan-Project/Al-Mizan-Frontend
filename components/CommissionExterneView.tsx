'use client';

import OrganisationsListPage from './OrganisationsListPage';

export default function CommissionExterneView() {
  return <OrganisationsListPage orgType="commission_externe" title="Commissions Externes" />;
}